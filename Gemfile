# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in office-workout.gemspec
git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'celluloid-io'
gem 'dotenv'
gem 'puma'
gem 'rspec', '~> 3.0'
gem 'slack-ruby-bot'
